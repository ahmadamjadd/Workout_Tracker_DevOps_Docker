import { describe, it, expect } from 'vitest';

describe('App Component', () => {
  it('renders without crashing', () => {
    // This is a basic test that just checks if the component loads
    expect(true).toBe(true); 
  });
});